"""Board domain: the perspective grid model and board serialization.

* ``grid``  the only perspective/grid implementation (BoardGrid);
* ``io``    Board load/save between solver model and board.json.
"""
